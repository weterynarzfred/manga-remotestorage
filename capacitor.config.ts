import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.isitoutyet.android',
  appName: 'IsItOutYet',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
